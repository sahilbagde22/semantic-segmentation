# semantic-segmentation
A deep learning based Semantic Segmentation project for pixel-wise classification of images. Implements model training, evaluation, and visualization on a custom dataset using convolutional neural networks.
